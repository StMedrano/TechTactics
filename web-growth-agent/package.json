{
  "name": "@techtactics/web-growth-agent",
  "version": "0.1.0",
  "private": true,
  "description": "TechTactics local-business website opportunity scouting, auditing, demo, and proposal agent.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/src/cli.js serve",
    "wga": "tsx src/cli.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "zod": "^3.24.1",
    "@google/genai": "^2.24.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
